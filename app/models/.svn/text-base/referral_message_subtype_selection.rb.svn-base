#
# Tracks one or more subtypes for a message selected for a ReferralMessage
#
#
#
class ReferralMessageSubtypeSelection < ActiveRecord::Base
  uses_guid
  acts_as_paranoid
  belongs_to :referral_message
  belongs_to :referral_message_subtype
end
