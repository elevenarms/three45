class CreateReferralNoteContentTypes < ActiveRecord::Migration
  def self.up
    # outdated table - not creating, just dropping if found
  end

  def self.down
    drop_table :referral_note_content_types rescue puts "ignoring - table not found"
  end
end
